
#include <bits/stdc++.h>
using namespace std;

void imp(){

  #ifndef ONLINE_JUDGE
      freopen("input.txt","r",stdin);
      freopen("output.txt","w",stdout);
  #endif
}

int main () {
    imp ();
    vector<int> arr;
    int n, s;

    cin >> n >> s;

    for (int i = 0; i < n; i++) {
        int tmp;
        cin >> tmp;

        arr.push_back (tmp);
    }
}